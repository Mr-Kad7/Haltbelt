'use client';
import {useState} from 'react'; import {supabase} from '../../lib/supabase';
export default function Page(){const [password,setPassword]=useState(''),[message,setMessage]=useState('');async function update(e:React.FormEvent){e.preventDefault();const {error}=await supabase().auth.updateUser({password});setMessage(error?error.message:'Password updated. You can now continue your recovery journey.');if(!error)setTimeout(()=>location.href='/dashboard',800)}return <main className="container page narrow"><div className="eyebrow"><span/> SECURE ACCOUNT</div><h1>Choose a new <span className="red">password.</span></h1><form className="card form-card" onSubmit={update}><label className="label">New password<input className="input" type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="btn btn-red">Update password</button>{message&&<p className="notice">{message}</p>}</form></main>}
