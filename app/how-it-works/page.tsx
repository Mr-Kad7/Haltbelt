export default function Page(){return <main className="container" style={{padding:"80px 24px"}}><h1>How Recovery Path Works</h1><p className="muted" style={{maxWidth:700,fontSize:18}}>The platform is organized around the moments that matter most.</p><div className="grid grid3" style={{marginTop:35}}>{["Prevent — understand triggers and reduce access.","Intervene — use the 15-minute urge pathway.","Stabilize — break the loss-chasing cycle.","Rebuild — work on debt, budgeting and savings.","Thrive — rebuild routines, relationships and goals."].map((x,i)=><div className="card" key={x}><span className="red">0{i+1}</span><h2>{x.split(" — ")[0]}</h2><p className="muted">{x.split(" — ")[1]}</p></div>)}</div></main>}