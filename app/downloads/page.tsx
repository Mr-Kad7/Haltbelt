export default function Page(){return <main className="container" style={{padding:"70px 24px"}}><span className="pill">DOWNLOAD RECOVERY PATH</span><h1>Recovery Path on your devices.</h1><p className="muted" style={{maxWidth:760,fontSize:18}}>One recovery platform across phone, desktop and browser. Store-specific installers will be published after signing, testing and review.</p><div className="grid grid3" style={{marginTop:35}}>{[["Android","Google Play / APK","Android app package via Capacitor. Play Store publication requires a signed AAB."],["iPhone & iPad","Apple App Store / TestFlight","iOS package via Capacitor. Requires Xcode and Apple signing."],["Windows","Windows installer","Signed EXE/MSIX desktop package."],["Mac","macOS app","Signed and notarized DMG/APP package."],["Linux","Linux packages","AppImage/deb distribution."],["Chrome / Chromebook","Install from browser","Installable PWA from the deployed HTTPS website."]].map(x=><div className="card" key={x[0]}><span className="pill">{x[0]}</span><h2>{x[1]}</h2><p className="muted">{x[2]}</p><button className="btn btn-dark" disabled style={{opacity:.6}}>Coming after release build</button></div>)}</div><div className="card danger" style={{marginTop:30}}><h2>Why are the download buttons not live yet?</h2><p className="muted">Native apps must be compiled, signed, tested, and then submitted to the relevant stores. We should not publish unsigned or unverified builds, especially for a recovery platform handling sensitive information.</p></div></main>}