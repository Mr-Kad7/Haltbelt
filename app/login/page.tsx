'use client';
import { useState } from 'react'; import Link from 'next/link'; import { supabase } from '../../lib/supabase';
export default function Page(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[message,setMessage]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMessage('');const {error}=await supabase().auth.signInWithPassword({email,password});setBusy(false);if(error){setMessage(error.message);return;}location.href='/dashboard';}return <main className="container page narrow"><div className="eyebrow"><span/> WELCOME BACK</div><h1>Back to <span className="red">your path.</span></h1><p className="muted">Pick up where you left off.</p><form className="card form-card" onSubmit={submit}><label className="label">Email<input required type="email" className="input" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="label">Password<input required type="password" className="input" value={password} onChange={e=>setPassword(e.target.value)}/></label><button disabled={busy} className="btn btn-red full">{busy?'Signing in…':'Sign in'}</button>{message&&<p className="notice">{message}</p>}<p className="muted small">New here? <Link href="/signup">Create an account →</Link></p></form></main>}
