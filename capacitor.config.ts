import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId:'com.recoverypath.app', appName:'Hatbet by Eric', webDir:'.next', server:{androidScheme:'https'} };
export default config;
